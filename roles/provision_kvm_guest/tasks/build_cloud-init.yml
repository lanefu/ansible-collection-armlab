---

- name: render meta-data
  template:
    src: meta-data.j2
    dest: "{{ provision_kvm_guest_machine_folder }}/{{ provision_kvm_guest_hostname }}-meta-data"
  tags:
    - cloud-init
    - meta-data

- name: render user-data
  template:
    src: user-data.j2
    dest: "{{ provision_kvm_guest_machine_folder }}/{{ provision_kvm_guest_hostname }}-user-data"
  tags:
    - cloud-init
    - user-data
